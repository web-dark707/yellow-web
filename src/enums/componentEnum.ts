/**
 * @description: 组件类型
 */

export enum FormInternalComponentType {
    Input = 'Input',
    Textarea = 'Textarea',
    Checkbox = 'Checkbox',
    Radio = 'Radio',
}

export enum DatePickerMode {
    DATE = 'date', // 年月日
    TIME = 'time', // 时分
    DATETIME = 'datetime', // 年月日时分
    YEAR = 'year', //年
    MONTH = 'month', // 月
}
