import React from 'react';
import VideoPlayer from './components/VideoPlayer';

const Player = () => {
    return (
        <div>
            <VideoPlayer />
        </div>
    );
};

export default Player;
